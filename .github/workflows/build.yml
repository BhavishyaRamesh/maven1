name: Java CI

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
      - name: Set up JDK 11
        uses: actions/setup-java@v3
        with:
          java-version: '11'
          distribution: 'adopt'
      - name: Build with Maven
        run: mvn --batch-mode --update-snapshots package
      - name: SonarQube Scan
        uses: kitabisa/sonarqube-action@v1.1.0
        with:
          host:  https://24db-2409-4071-d97-ea4a-e5d3-5cbb-cda0-5ca0.ngrok.io
          login: 67f202b16ac65225722a61ae40e846e1dfb5cb32
      - name: check java home
        run: echo $JAVA_HOME
